---
id: APR-00005
artifact: TASK-006
actor: human
decision: approved
timestamp: "2026-08-23"
notes: >
  production_approval_policy: human_required gate, cleared for real via /sprout:release. Release
  candidate independently re-verified before this approval was requested, not trusted from the
  task's own status field: MERGED confirmed against actual git history (commit 5b85c81 on
  origin/main, PR #6 — RUN-toy-002.md's recorded 8cff5aaa is the pre-squash-merge worktree commit,
  diff confirmed byte-identical), VERIFIED confirmed via RUN-toy-002.md's PASS verdict and
  EVD-toy-002.md's real test output. No staging deploy executed — tests/fixtures/toy-app has no
  real deployment mechanism, correctly not fabricated (docs/protocol.md §10: Sprout doesn't own
  deployment infra), and the task's own verification plan (unit, security) never called for a
  staging check. This is Sprout's first real exercise of /sprout:release end-to-end
  (docs/protocol.md §9 backlog item) — proves the human_required gate actually stops for approval
  rather than proceeding on its own.
---
